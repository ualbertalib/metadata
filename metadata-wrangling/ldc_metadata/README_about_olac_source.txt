The original source file of olac metadata is

metadata/metadata-wrangling/ldc_metadata/olac_source_olacdc.xml

It exceeds GitHub's filesize limit, and so it is excluded from the local index of this repository through its inclusion in .git/info/exclude
